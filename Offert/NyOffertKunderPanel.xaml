<UserControl x:Class="FaVSD.NyOffertKunderPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
       
        <StackPanel>

            <TextBlock Text="Kund"/>

            <TextBox x:Name="KundNamnTextLada"
                Text="KundNamn"/>

            <ComboBox x:Name="KundTypComboLada" 
                Margin="0,0,0,20"/>

            <TextBox Text="KontaktPerson"
                x:Name="KontaktPersonTextLada"/>

            <TextBox x:Name="KontaktMejlTextLada"
                Text="KontaktMejl"
                Margin="0,0,0,20"/>

            <TextBox x:Name="FakturaAddress" 
                Text="FakturaAddressTextLada"/>

            <TextBox x:Name="LeveransAddress" 
                Text="LeveransAddressTextLada"/>

            <Button Content="Spara kund" 
                Click="LaggTillKundKlick"/>
            
        </StackPanel>
    </Grid>
</UserControl>