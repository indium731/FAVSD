<UserControl x:Class="FaVSD.MenyVy"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FaVSD"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.ColumnSpan="2"
                Text="SpelCenter" />
        
        <StackPanel Grid.Row="0" Grid.Column="0">

            <Button Content="Ny offert"
                x:Name="NyOffert"
                Click="GaTillValdVy">
                <Button.Tag>
                    <local:NyOffertVy/>
                </Button.Tag>
            </Button>

            <Button Content="Ny order"
                x:Name="NyOrder"
                Click="GaTillValdVy">
                <Button.Tag>
                    <local:NyOrderVy/>
                </Button.Tag>
            </Button>
            
            <Button Content="PlocklistaHanterare"
                x:Name="PlocklistaHanterare"
                Click="GaTillValdVy">
                <Button.Tag>
                    <local:PlocklistaVy/>
                </Button.Tag>
            </Button>
            


        </StackPanel>

        <StackPanel Grid.Row="0" Grid.Column="1">

            <Button Content="Logga ut" 
                Click="TestaUtloggKlick"/>

        </StackPanel>
    </Grid>
</UserControl>